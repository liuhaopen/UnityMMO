local skynet = require "skynet"
require "Common.Util.util"

local skill = {}

function skill.skill_get_role_skill_list(user_info, req_data)

end

function skill.skill_get_role_skill_list_in_ui(user_info, req_data)
	
end

return skill
